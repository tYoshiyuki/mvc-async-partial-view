@{
    ViewData["Title"] = "Home Page";
}

<div class="partial-contents position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center bg-light" data-action="@Url.Action("topcontents")">
</div>

<div class="partial-contents d-md-flex flex-md-equal w-100 my-md-3 pl-md-3" data-action="@Url.Action("subcontents")">
</div>
